<table class = "table">
  <thead>
    <tr>
      <th>商品ID</th>
      <th>商品名</th>
      <th>税抜価格</th>
      <th>ジャンル</th>
      <th>ステータス</th>
   </tr>
  </thead>
<% items.each do |item| %>
  <tbody>
    <tr>
      <td>
        <%= link_to admin_item_path(item.id) do %>
          <%= item.id %>
        <% end %>
      </td>
      <td><%= item.name %></td>
      <td><%= item.price %></td>
      <td> item.genre </td>
      <td> <%= item.is_status ? '販売中' : '販売停止中'  %> </td>
    </tr>
  </tbody>
  <% end %>
</table>
<div class = "justify-content-center" >
  <%= paginate items %>
</div>