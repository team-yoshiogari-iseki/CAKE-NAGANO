<div class="container">
  <div class="row-md-12 mt-5">
    <h2 class="ml-5 col-md-3 text-center d-inline" style="background-color:rgba(205, 197, 197, 0.53);">注文履歴一覧</h2>
    <table class="table table-bordered border-dark mt-5">
      <thead>
        <tr class="active" style="background-color:rgba(107,90,105,0.1);">
          <th class="border-dark">注文日</th>
          <th class="border-dark">配送先</th>
          <th class="border-dark">注文商品</th>
          <th class="border-dark">支払金額</th>
          <th class="border-dark">ステータス</th>
          <th class="border-dark">注文詳細</th>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
        <tr>
          <td class="border-dark"><%= order.created_at %></td>
          <td class="border-dark">
            <%= order.postal_code %>
            <%= order.address %>
            <%= order.orderer_name %>
          </td>
          <td class="border-dark">
            <% order.order_details.each do |order_detail| %>
            <%= order_detail.item.name %>
            <% end %>
          </td>
          <td class="border-dark"><%= order.claimed %></td>
          <td class="border-dark"><%= order.order_status %></td>
          <td class="border-dark"><%= link_to "表示する", order_path(order), class: 'btn btn-primary' %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>